# d.py [cobalt](https://cobalt.tools/) bot

## setup
create a `.env` file like this:
```
TOKEN=<discord token here>
```

install the dependencies with
```
python -m pip install -r requirements.txt
```